const services = window.SOLVIX_SERVICES;
const pathways = window.SOLVIX_PATHWAYS;
const header=`<header><div class="wrap"><a class="brand" href="./" aria-label="SOLVIX Research Hub home"><img class="emblem" src="./assets/research-emblem.png" alt="SOLVIX Research Hub emblem"></a><nav aria-label="Main navigation"><a href="#about">Our purpose</a><a href="#applications">PhD & internships</a><a href="#services">Services</a><a href="#events">Events</a><a class="nav-cta" href="#contact">Connect with us ↗</a></nav></div></header>`;
const footer=`<footer id="contact"><div class="wrap"><div class="footer-top"><div><span class="eyebrow">SOLVIX RESEARCH HUB · PART OF SOLVIX LEARNING</span><h2>Bring your questions.<br>Build your research direction.</h2><p>Connect for research guidance, scientific writing support and upcoming programmes.</p></div><div><div class="socials">${Object.entries(window.SOLVIX_CONTACTS).map(([name,url])=>safeUrl(url)?`<a class="social" href="${escapeHtml(safeUrl(url))}">${escapeHtml(name)} ↗</a>`:`<button class="social" data-social="${escapeHtml(name)}">${escapeHtml(name)} ↗</button>`).join('')}</div><p class="contact-note">${Object.values(window.SOLVIX_CONTACTS).some(safeUrl)?'Connect through our available channels.':'Official contact channels will be announced.'}</p></div></div><div class="footer-bottom"><span>© ${new Date().getFullYear()} SOLVIX Learning · Research Hub</span><span>Question. Investigate. Communicate.</span></div></div></footer><dialog id="details" aria-labelledby="dialog-title"><button class="close" aria-label="Close dialog">×</button><div id="dialog-content"></div></dialog>`;
const main=`<main id="main-content"><section class="hero"><div class="hero-art"></div><div class="wrap"><div class="hero-content"><img class="hero-emblem" src="./assets/research-emblem.png" alt="SOLVIX Research Hub"><span class="eyebrow">SOLVIX LEARNING / RESEARCH HUB</span><h1>From a question<br><em>to meaningful research.</em></h1><p>Research guidance, scientific writing, and PhD and internship application preparation for students and early-career researchers.</p><div class="actions"><a class="btn primary" href="#guidance">Explore research guidance ↗</a><a class="btn" href="#applications">PhD & internship preparation</a></div></div></div><div class="hero-bottom"><span>A DEDICATED SPACE FOR RESEARCH & SCIENTIFIC COMMUNICATION</span><span>SCROLL TO EXPLORE ↓</span></div></section><div class="science-strip"><div class="wrap"><span>RESEARCH DESIGN</span><span>LITERATURE REVIEWS</span><span>SCIENTIFIC WRITING</span><span>DATA ANALYSIS</span><span>PhD & INTERNSHIP APPLICATIONS</span></div></div><section id="about" class="section wrap mission"><div><span class="eyebrow">OUR PURPOSE</span><h2>Build the skills.<br>Develop your own research.</h2></div><div class="mission-copy"><article><h3>Our mission</h3><p>To support students and early-career researchers in framing questions, designing studies and communicating their work through practical guidance, mentorship and preparation for PhD and research internship applications.</p></article><article><h3>Our vision</h3><p>A research community where people across disciplines can develop independent thinking, sound methods and the confidence to share knowledge.</p></article></div></section><section class="team-note wrap" aria-label="Our organising community"><span class="eyebrow">OUR ORGANISING COMMUNITY</span><p>Programmes planned and delivered by PhD students from India and abroad, together with IIT fellows.</p></section><section class="section wrap pathways" style="padding-top:15px"><div class="section-head"><div><span class="eyebrow">YOUR RESEARCH, STEP BY STEP</span><h2>Support at every stage.</h2></div><p class="desc">Start with your question, strengthen your methods and communicate what you find.</p></div><div class="topic-grid">${pathways.map(([n,t,d,id])=>`<a class="path-card" href="#${id}"><span class="eyebrow">${n} / EXPLORE</span><h3>${t}</h3><p>${d}</p><span class="hub-link">Explore support ↗</span></a>`).join('')}</div></section>${pathways.map(([n,t,d,id,title,description,topics])=>`<section id="${id}" class="research-detail section ${id==='writing'?'services':''}"><div class="wrap detail-grid"><div><span class="eyebrow">${n} / ${t}</span><h2>${title}</h2><p class="desc">${description}</p></div><div class="topic-list">${topics.map((topic,i)=>`<article><span>0${i+1}</span><h3>${topic}</h3></article>`).join('')}</div></div></section>`).join('')}<section id="services" class="section services"><div class="wrap"><div class="section-head"><div><span class="eyebrow">OUR SERVICES</span><h2>Learn with guidance.<br>Progress through practice.</h2></div><p class="desc">Focused support for your research journey. Programme availability and formats will be announced.</p></div><div class="service-grid">${services.map(([icon,t,d])=>`<article class="service"><div class="icon" aria-hidden="true">${icon}</div><h3>${t}</h3><p>${d}</p></article>`).join('')}</div><p class="support-note">Mentorship and writing support help you develop your own work, with attention to research integrity, accurate citations and transparent methods.</p></div></section><section id="events" class="section wrap"><div class="section-head"><div><span class="eyebrow">RESEARCH EVENTS & WORKSHOPS</span><h2>Your next step starts here.</h2></div><span class="preview-label"><span id="schedule-label">Research schedule</span></span></div><div class="calendar-layout"><div class="calendar"><div class="calendar-nav"><strong id="month"></strong><div><button id="prev" aria-label="Previous month">‹</button> <button id="next" aria-label="Next month">›</button></div></div><div class="calendar-grid" id="calendar"></div><p class="cal-note">Select a highlighted date for event details. Planned events are marked; final arrangements will be announced.</p></div><div id="event-list"></div></div></section></main>`;
document.getElementById('app').innerHTML=header+main+footer;
const dialog=document.getElementById('details');function openDialog(html){document.getElementById('dialog-content').innerHTML=html;const title=dialog.querySelector('h2');if(title)title.id='dialog-title';dialog.showModal()}dialog.querySelector('.close').onclick=()=>dialog.close();dialog.onclick=e=>{if(e.target===dialog)dialog.close()};document.querySelectorAll('[data-social]').forEach(b=>b.onclick=()=>openDialog(`<span class="eyebrow">CONNECT WITH SOLVIX RESEARCH HUB</span><h2>${b.dataset.social}</h2><p>Our official ${b.dataset.social} contact will be announced here.</p>`));

const events = window.SOLVIX_EVENTS;
function escapeHtml(value) { return String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }
function safeUrl(value) { return /^(https:\/\/|mailto:)/i.test(value || '') ? value : ''; }
const now = new Date();
const upcoming = events.filter(e => e.date >= [now.getFullYear(),String(now.getMonth()+1).padStart(2,'0'),String(now.getDate()).padStart(2,'0')].join('-')).sort((a,b)=>a.date.localeCompare(b.date));
const firstDate = upcoming.length ? new Date(upcoming[0].date+'T12:00:00') : now;
let year=firstDate.getFullYear(), month=firstDate.getMonth(), selected=null;
function dateKey(day) { return `${year}-${String(month+1).padStart(2,'0')}-${String(day).padStart(2,'0')}`; }
function showEvent(e) {
  const date = new Date(e.date+'T12:00:00').toLocaleDateString('en',{day:'numeric',month:'long',year:'numeric'});
  const link = !e.example && e.status === 'confirmed' && safeUrl(e.registrationUrl);
  openDialog(`<span class="eyebrow">${e.example?'EXAMPLE EVENT · NOT CONFIRMED':e.status==='planned'?'PLANNED WORKSHOP · DETAILS TO FOLLOW':'RESEARCH EVENT'}</span><h2>${escapeHtml(e.title)}</h2><p>${escapeHtml(e.description)}</p><p><strong>${e.example?'Illustrative date':'Date'}:</strong> ${date}<br><strong>Format:</strong> ${escapeHtml(e.format)}<br>${escapeHtml(e.details)}</p>${link?`<a class="btn primary" href="${escapeHtml(link)}">Register for this event ↗</a>`:'<p>Registration will become available after programme details are confirmed.</p>'}`);
}
function render() {
  const label = new Date(year,month).toLocaleDateString('en',{month:'long',year:'numeric'});
  document.getElementById('month').textContent = label;
  document.getElementById('schedule-label').textContent = events.length && events.every(e=>e.example) ? 'Preview schedule · example events' : events.some(e=>e.status==='planned') ? 'Upcoming · planned events' : 'Research schedule';
  const active=events.filter(e=>e.date.startsWith(dateKey(1).slice(0,7)));
  let html=['Mon','Tue','Wed','Thu','Fri','Sat','Sun'].map(d=>`<span class="weekday">${d}</span>`).join('');
  html+='<span></span>'.repeat((new Date(year,month,1).getDay()+6)%7);
  for(let day=1;day<=new Date(year,month+1,0).getDate();day++) {
    const dayEvents=active.filter(e=>e.date===dateKey(day));
    html+=`<button class="day ${dayEvents.length?'event':''} ${selected===day?'selected':''}" data-day="${day}" aria-pressed="${selected===day}" aria-label="${day} ${label}${dayEvents.length?', '+dayEvents.length+' event(s)':''}">${day}</button>`;
  }
  document.getElementById('calendar').innerHTML=html;
  const shown=selected ? active.filter(e=>e.date===dateKey(selected)) : active;
  document.getElementById('event-list').innerHTML=shown.length?shown.map(e=>`<button class="event-card" data-event="${events.indexOf(e)}"><span class="date">${Number(e.date.slice(8))}<small>${new Date(e.date+'T12:00:00').toLocaleDateString('en',{month:'short'})}</small></span><span><span class="tag">${e.example?'Example · ':e.status==='planned'?'Planned · ':''}${escapeHtml(e.type)}</span><h3>${escapeHtml(e.title)}</h3><span class="event-meta">${escapeHtml(e.format)} · View details ↗</span></span></button>`).join(''):`<p class="event-empty">${selected?'No events scheduled for this date. Select another date or change the month.':'No events announced for this month.'}</p>`;
  document.querySelectorAll('[data-day]').forEach(b=>b.onclick=()=>{const day=Number(b.dataset.day);selected=selected===day?null:day;render();document.querySelector(`[data-day="${day}"]`).focus();const matches=active.filter(e=>e.date===dateKey(selected));if(matches.length===1)showEvent(matches[0]);});
  document.querySelectorAll('[data-event]').forEach(b=>b.onclick=()=>showEvent(events[Number(b.dataset.event)]));
}
render();
document.getElementById('prev').onclick=()=>{month--;if(month<0){month=11;year--}selected=null;render();};
document.getElementById('next').onclick=()=>{month++;if(month>11){month=0;year++}selected=null;render();};
if(!matchMedia('(prefers-reduced-motion: reduce)').matches){document.querySelector('.hero').addEventListener('pointermove',e=>{document.querySelector('.hero-art').style.transform=`translate(${(e.clientX/innerWidth-.5)*12}px,${(e.clientY/innerHeight-.5)*10}px) scale(1.025)`;});}
